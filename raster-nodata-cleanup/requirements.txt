# Standalone batch mode
rasterio>=1.3

# Demo only (example_usage.py)
numpy>=1.24
matplotlib>=3.7

# QGIS Console mode requires no separate install —
# it uses the qgis.core API bundled with QGIS Desktop.
