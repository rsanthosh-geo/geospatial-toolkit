# Standalone mode
rasterio>=1.3
numpy>=1.24
scikit-image>=0.26
shapely>=2.0
geopandas>=0.14

# Demo only (example_usage.py)
matplotlib>=3.7

# QGIS mode requires no separate install —
# it uses the qgis.core / processing API bundled with QGIS Desktop.
